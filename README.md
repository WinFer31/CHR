# CHR

CHR PID tuning method.

MATLAB code to tune P, PI, PID controller parameters from the step response of a system using the Chien-Hrones-Reswick (CHR) for open loop step response with any given transfer function.
